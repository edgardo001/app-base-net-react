# frontend-core

Aplicación React 19 + Vite + TypeScript + Tailwind CSS v4 + shadcn/ui, layout completo (sidebar, header), auth store con Zustand, API client con axios, páginas (login, dashboard, users, roles, profile, admin, change-password), session warning modal, componentes UI shadcn
